target-version = "py310"
line-length = 120

[lint]
# Mantenemos el set por defecto de Ruff; solo ajustamos excepciones por archivo.

[lint.per-file-ignores]
# En settings de Django es común tener imports tardíos y star-imports.
"backend/src/config/settings/*.py" = ["E402", "F403", "F405"]
