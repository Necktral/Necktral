name: PM Snapshot

on:
  push:
    branches: ["main", "master"]
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *" # diario 03:00 UTC

concurrency:
  group: pm-snapshot-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  actions: read
  pull-requests: write
  issues: write

jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout (full history)
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@f677139bbe7f9c59b41e40162b753c062f5d49a3 # v5.2.0
        with:
          python-version: "3.10"
          cache: "pip"
          cache-dependency-path: |
            requirements/base.txt
            requirements/dev.txt

      - name: Install backend dependencies
        continue-on-error: true
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements/dev.txt

      - name: QA - init output files
        if: always()
        run: |
          : > qa_ruff.txt
          : > qa_pytest.txt
          : > qa_coverage.txt
          : > qa_django_check.txt
          : > qa_makemigrations_check.txt

      # QA (captura aunque falle)
      - name: QA - ruff
        id: qa_ruff
        if: always()
        continue-on-error: true
        run: |
          set -o pipefail
          ruff check . 2>&1 | tee qa_ruff.txt

      - name: QA - pytest + coverage
        id: qa_pytest
        if: always()
        continue-on-error: true
        working-directory: backend
        run: |
          set -o pipefail
          coverage run -m pytest -q --maxfail=1 --disable-warnings 2>&1 | tee ../qa_pytest.txt
          coverage report 2>&1 | tee ../qa_coverage.txt

      - name: QA - django checks
        id: qa_django
        if: always()
        continue-on-error: true
        working-directory: backend
        run: |
          set -o pipefail
          python manage.py check 2>&1 | tee ../qa_django_check.txt
          python manage.py makemigrations --check --dry-run 2>&1 | tee ../qa_makemigrations_check.txt

      - name: QA - write outcomes
        if: always()
        run: |
          {
            echo "qa_ruff_outcome=${{ steps.qa_ruff.outcome }}";
            echo "qa_pytest_outcome=${{ steps.qa_pytest.outcome }}";
            echo "qa_django_outcome=${{ steps.qa_django.outcome }}";
          } > qa_outcomes.txt

      - name: Generate PM snapshot
        if: always()
        continue-on-error: true
        env:
          PM_PR_NUMBER: ${{ github.event.pull_request.number || '' }}
          PM_PR_URL: ${{ github.event.pull_request.html_url || '' }}
          PM_BASE_REF: ${{ github.event.pull_request.base.ref || '' }}
          PM_BASE_SHA: ${{ github.event.pull_request.base.sha || '' }}
          PM_HEAD_REF: ${{ github.event.pull_request.head.ref || '' }}
          PM_HEAD_SHA: ${{ github.event.pull_request.head.sha || '' }}
        run: |
          bash scripts/pm_snapshot.sh pm_snapshot.md

      - name: Ensure pm_snapshot.md exists
        if: always()
        run: |
          if [ ! -f pm_snapshot.md ]; then
            {
              echo "# PM Snapshot";
              echo;
              echo "(snapshot generation failed; placeholder)";
            } > pm_snapshot.md
          fi

      - name: Publish snapshot to job summary (truncated)
        if: always()
        run: |
          echo "## PM Snapshot" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          sed -n '1,220p' pm_snapshot.md >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "_(Truncated. Descarga el artefacto **pm-snapshot** para el archivo completo.)_" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload snapshot artifact
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pm-snapshot
          path: |
            pm_snapshot.md
            qa_ruff.txt
            qa_pytest.txt
            qa_coverage.txt
            qa_django_check.txt
            qa_makemigrations_check.txt
            qa_outcomes.txt
          if-no-files-found: warn
          retention-days: 30

      - name: Upsert PR comment (PM snapshot)
        if: >
          github.event_name == 'pull_request' &&
          github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name
        uses: actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea # v7.0.1
        with:
          script: |
            const fs = require('fs');
            const marker = '<!-- PM_SNAPSHOT -->';

            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const prNumber = context.payload.pull_request.number;

            const runUrl = `${context.serverUrl}/${owner}/${repo}/actions/runs/${context.runId}`;
            const prUrl = context.payload.pull_request.html_url;

            const raw = fs.existsSync('pm_snapshot.md') ? fs.readFileSync('pm_snapshot.md', 'utf8') : '';
            const max = 10000; // evitar límites / ruido
            const snippet = raw.length > max ? raw.slice(0, max) + '\n\n_(Truncated)_\n' : raw;

            const body = [
              marker,
              'PM Snapshot (auto-actualizado).',
              '',
              `- PR: ${prUrl}`,
              `- Run: ${runUrl}`,
              '',
              '<details><summary>Snapshot</summary>',
              '',
              '```md',
              snippet,
              '```',
              '',
              '</details>',
              '',
              'Artefacto: **pm-snapshot** (pm_snapshot.md) en este run.',
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner,
              repo,
              issue_number: prNumber,
              per_page: 100,
            });

            const existing = comments.find(c => (c.body || '').includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({ owner, repo, comment_id: existing.id, body });
            } else {
              await github.rest.issues.createComment({ owner, repo, issue_number: prNumber, body });
            }
